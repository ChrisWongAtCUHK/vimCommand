" encoding
set encoding=utf-8
set nu

" The following command puts the contents of ~/lengend_header.txt under the cursor when types ":Legend" normal mode. IMPORTANT: the first character must be capital letter
command Legend :r ~/legend_header.txt


command Chriswong :r ~/chriswong.txt

